package org.tensorflow.keras.logs;

/**
 * Base class for representing information
 * collected during a portion of a model's 
 * training loop (e.g. train / val batch losses, accuracy)
 */
public abstract class Logs {}